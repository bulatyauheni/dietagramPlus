<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:dither="true"
    android:shape="rectangle"
    android:useLevel="false"
    android:visible="true">
    <gradient
        android:endColor="@color/bg_group_item_dragging_state"
        android:startColor="@color/bg_group_item_dragging_state"/>
    <corners android:radius="15dp" />
    <size
        android:width="200dp"
        android:height="30dp" />
    <stroke android:width="1dp" android:color="@color/bg_group_item_dragging_state" />
</shape>